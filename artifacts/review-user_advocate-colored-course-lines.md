# User-advocate review - colored course lines

PASS

## Verdict

No high, medium, or low findings.

`ack_zero_findings_reason`: The UI now gives users an understandable default course-chain view, explicit controls to broaden the scope, discoverable branch and shared-bus relationship controls, clear relationship explanations, and reversible selection/reset behavior. I found no new task-related usability blocker or confusing interaction from static inspection and the settled phase-1 review evidence.

## Scope And Limitation

I reviewed `c01c837..92479b9` after the tech-lead and project-manager artifacts had settled as PASS. I did not run a live browser, visual screenshot, or pointer smoke test because this agent has no CUA browser/app surface available. This review therefore uses static code inspection plus the parent-reported successful `npm.cmd test`, `npm.cmd run typecheck`, and `npm.cmd run build` verification.

## User Job Fit

The requested user job is to select a course and understand only that course's dependency graph, with stable colored lines that can be clicked to explain whether the user selected one exact prerequisite branch or the grouped visible prerequisites for a destination.

That job is covered. The default scope is now `"course"` at `components/course-graph.tsx:95`, and the selected course graph comes from `visibleGraphDistances("course")`, which returns `directedCourseChain` at `lib/graph.ts:105`. `directedCourseChain` walks only directed prerequisite ancestors and downstream unlocks while skipping corequisites at `lib/graph.ts:324`, so the default view avoids sibling prerequisite noise. The scope selector still exposes broader views, including the entire program, at `components/course-graph.tsx:425`.

Stable destination colors are generated by `stableDestinationColor` at `lib/graph.ts:177` and applied to prerequisite arrows at `lib/graph.ts:237`. The user is not asked to infer meaning from color alone: the map hint explains selecting a colored branch versus a shared bus at `components/course-graph.tsx:451`, the footnote says colors group by destination and not AND/OR satisfaction at `components/course-graph.tsx:493`, and selecting a relationship renders text detail plus the target catalog rule at `components/course-graph.tsx:467`.

## Interaction Notes

- Discoverability: The page heading states the course-selection workflow, the map hint names branch and bus selection, the inspector lists native controls under "Visible map relationships", and the scope select labels the default as "This course's full chain" at `components/course-graph.tsx:425`.
- Reversibility: Users can clear a relationship with the visible Clear button at `components/course-graph.tsx:472`, press Escape from inside the graph panel at `components/course-graph.tsx:288`, change scope at `components/course-graph.tsx:426`, or select another course, which clears stale relationship state and resets to course focus at `components/course-graph.tsx:300`.
- Usefulness: Branch selection keeps the exact source/target pair, while bus selection includes every visible incoming source for the selected destination, through `relationshipSelection` at `lib/graph.ts:209`. This matches the user's need to distinguish one line from a grouped prerequisite bus.
- Error and empty states: Empty relationship sets show "This course has no prerequisite or unlock relationships in this catalog" at `components/course-graph.tsx:471`; search miss copy says "No matching courses. Try another code or title" at `components/course-graph.tsx:395`.
- Accessibility: Keyboard users can operate relationship selection through real inspector buttons with `aria-pressed` state at `components/course-graph.tsx:468`. The graph has a skip link at `components/course-graph.tsx:413`, a table alternative at `components/course-graph.tsx:421`, a scrollable canvas label at `components/graph-canvas.tsx:83`, and non-color textual descriptions for selected relationships at `components/course-graph.tsx:467`.

## Shneiderman Notes

- Consistency: Scope, map/table, search, history, and inspector actions use existing button/select patterns in this component.
- Shortcuts for frequent users: Search supports Ctrl/Cmd+F, Enter, arrows, and Escape through `mapFind` handling at `components/course-graph.tsx:263` and the input handlers at `components/course-graph.tsx:357`.
- Informative feedback: The graph status line reports course count, arrow count, selected course, and active selected relationship at `components/course-graph.tsx:420`.
- Closure: Selecting a relationship produces a clear inspector explanation and catalog rule at `components/course-graph.tsx:467`.
- Simple error handling: Empty graph relationships and no search matches have direct recovery copy at `components/course-graph.tsx:395` and `components/course-graph.tsx:471`.
- Easy reversal: Clear, Escape, course navigation, and scope changes clear relationship state without a destructive side effect at `components/course-graph.tsx:288`, `components/course-graph.tsx:300`, and `components/course-graph.tsx:426`.
- Internal locus of control: Users explicitly choose map/table, overview, scope, course, branch, or bus; selecting a course focuses that course and clears stale context.
- Reduced short-term memory load: Relationship groups and exact branches remain listed in the inspector, and selected relationship copy repeats the relevant source, target, and catalog rule.
- Clarity and visual hierarchy: The selected/focused relationship is represented by inspector copy, highlighted nodes, dimmed unrelated nodes, and edge opacity at `components/course-graph.tsx:136` and `components/course-graph.tsx:230`.
- Accessibility and color: Color is stable and useful, but the UI also provides labels, buttons, aria states, and text explanations, so the primary relationship meaning is not color-only.

## Settled Review Alignment

I agree with the prior PASS artifacts. The previous high concerns are resolved from a user-facing perspective: shared-bus selection is no longer ambiguous because branch and bus hit paths are separated in `prerequisiteHitPaths` at `lib/graph.ts:157` and wired separately in `components/graph-canvas.tsx:35`; keyboard users now have inspector buttons for both grouped bus and exact branch selection at `components/course-graph.tsx:468`.
